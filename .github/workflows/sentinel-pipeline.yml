name: Sentinel Frontend CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-frontend

jobs:
  test-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install deterministic dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run RBAC Security Audit (Vitest Coverage)
        working-directory: ./frontend
        run: npm run test:ci

      - name: Verify Production Next.js Build
        working-directory: ./frontend
        run: npm run build

  build-and-push:
    needs: test-and-verify
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push production image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

#   deploy-to-kubernetes:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     environment: production
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
# 
#       - name: Set up Kubectl
#         uses: azure/setup-kubectl@v3
# 
#       - name: Configure Kubernetes Context
#         uses: azure/k8s-set-context@v3
#         with:
#           method: kubeconfig
#           kubeconfig: ${{ secrets.KUBECONFIG_DATA }}
# 
#       - name: Deploy to K8s Cluster
#         run: |
#           # Update the image tag in the deployment manifest
#           sed -i "s|image: cloud-sentinel/frontend:latest|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${GITHUB_SHA::7}|g" infrastructure/kubernetes/frontend/deployment.yaml
#           
#           kubectl apply -f infrastructure/kubernetes/shared/
#           kubectl apply -f infrastructure/kubernetes/frontend/
#           
#       - name: Verify Deployment Rollout
#         run: |
#           kubectl rollout status deployment/sentinel-frontend -n platform-ops --timeout=120s
