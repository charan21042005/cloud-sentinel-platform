apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - datasources-config.yaml
  - dashboards-provider.yaml
  - grafana.yaml
  - service.yaml

commonLabels:
  platform.sentinel.io/component: grafana-ui
