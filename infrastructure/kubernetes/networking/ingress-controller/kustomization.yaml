apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - ingress-class.yaml
  - configmap-tuning.yaml
  - hpa-scaling.yaml

commonLabels:
  platform.sentinel.io/component: ingress-routing
