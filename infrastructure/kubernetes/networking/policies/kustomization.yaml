apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - default-deny-all.yaml
  - allow-ingress-controller.yaml
  - allow-monitoring-scraping.yaml
  - allow-internal-dns.yaml

commonLabels:
  platform.sentinel.io/component: network-isolation
