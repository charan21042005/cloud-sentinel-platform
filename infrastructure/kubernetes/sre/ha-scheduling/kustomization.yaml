apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patches:
  - path: topology-spread-patch.yaml
    target:
      kind: Deployment
      name: sentinel-api-gateway

commonLabels:
  platform.sentinel.io/component: sre-ha-scheduling
