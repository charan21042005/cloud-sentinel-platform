apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: sentinel-critical-infra
value: 1000000
globalDefault: false
description: "Used for core proxy ingress planes and SOC threat ingestion API gateways ensuring maximum scheduling priority."
---
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: sentinel-observability
value: 500000
globalDefault: false
description: "Used for persistent TSDB and Log chunk databases to ensure data is not evicted during node pressure."
