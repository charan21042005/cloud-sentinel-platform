apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - resource-quotas.yaml
  - limit-ranges.yaml
  - priority-classes.yaml

commonLabels:
  platform.sentinel.io/component: reliability-guardrails
