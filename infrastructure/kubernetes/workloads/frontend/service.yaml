apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: sentinel-apps
  labels:
    app.kubernetes.io/name: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: frontend
