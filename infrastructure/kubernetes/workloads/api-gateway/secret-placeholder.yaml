apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secrets-placeholder
  namespace: sentinel-apps
  labels:
    app.kubernetes.io/name: api-gateway
  annotations:
    sentinel.io/secret-strategy: "ExternalSecrets-Target"
    description: "Placeholder target mapping external cloud-vault dynamically injected keys"
type: Opaque
stringData:
  DATABASE_URL: "postgresql://sentinel:securepassword@sentinel-db.sentinel-apps.svc.cluster.local:5432/sentinelsoc"
  JWT_SECRET_KEY: "super-secret-enterprise-jwt-token-signing-signature-string"
